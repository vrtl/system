cargo-features = ["edition2024"]

[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
console_error_panic_hook = "0.1"
leptos = { version = "0.6", features = ["csr", "nightly"] }
wasm-bindgen = "0.2"
